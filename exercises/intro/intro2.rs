// intro2.rs
// Make the code print a greeting to the world.
// Execute `rustlings hint intro2` or use the `hint` watch subcommand for a hint.


fn main() {
    let var: &str = "World";
    let count: f64 = 2.0 / 3.0;
    println!("Hello {var}! {count} is not a very large number.");
}
